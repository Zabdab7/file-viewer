public class Main{
    public static void main(String[] args) {
        Frame f = new Frame();

        /*                          Todo List
         * - add hovering to the label class (transparent to momentarily blue)
         * - Fix Frame Class to do it's job
         * - Frame : Line 43 => possible null pointer exception in Util.getSuccessors(label.getFile()))
         */
    }
}
